<%= stylesheet_link_tag "mailers/welcome_mailer" %>
<div class="main-container-reset ff-welcome-mail">
  <div class="inner-container-reset">
    <div class="logo-container">
      <%= image_tag 'logo-mercately-h.png', class: "image-logo-reset" %>
    </div>

    <% if @errors.blank? %>
      <div class="ml-45 mt-20 mr-45">
        <p class="fs-16 p-color lg-18">
          La importación y creación del grupo de clientes culminó exitosamente. Puede verificar la información cargada en la lista de grupos de clientes.
        </p>
      </div>
    <% else %>
      <div class="ml-45 mt-20 mr-45">
        <p class="fs-16 p-color lg-18">
          Se agregaron <%= @contact_group.customers.count %> clientes al grupo.
        </p>
        <p class="fs-16 p-color lg-18">
          Hubo algunos clientes que no fue posible cargarlos por las siguientes razones:
        </p>
      </div>

      <div class="ml-45 mt-20 mr-45">
        <p class="fs-16 p-color lg-18">
          <ul>
            <% @errors.each do |err| %>
              <li class="py-5 p-color"><%= err %></li>
            <% end %>
          </ul>
        </p>
      </div>
    <% end %>

    <%= render "shared/emails/footer" %>
  </div>
</div>
