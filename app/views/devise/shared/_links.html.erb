<%- if controller_name != 'sessions' %>
  <p class="m-0">¿Ya tienes cuenta? <%= link_to "Iniciar sesión", new_session_path(resource_name), class: 'a-devise' %></p><br />
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p class="m-0">¿No tienes cuenta?<%= link_to " Registrate", new_registration_path(resource_name), class: 'a-devise' %></p><br />
<% end %>

<%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
  <p class="m-0"><%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class: 'a-devise' %></p><br />
<% end %>

<%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
  <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name) %><br />
<% end %>

<%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
  <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name) %><br />
<% end %>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %><br />
  <% end %>
<% end %>

<div id="social-media" class="mt-16 f-right mb-100">
  <%= image_tag 'facebook-logo.png', class: 'float-left social-media-icons' %>
  <%= image_tag 'twitter.png', class: 'float-left social-media-icons' %>
  <%= image_tag 'linkedin-logo.png', class: 'float-left social-media-icons' %>
</div>
