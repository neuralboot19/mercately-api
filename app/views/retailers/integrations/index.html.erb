<div class="container-fluid reduced-container-padding mb-16">
  <div class="card">
    <div class="container-fluid index">
      <div class="row middle-xs">
        <div class="col-xs-12 col-sm-12">
          <h1 class="d-inline name">Integraciones</h1>
          <div class="index__desc">
            Plataformas
          </div>
        </div>
      </div>
    </div>

    <% if current_retailer.meli_retailer && current_retailer.incomplete_meli_profile? %>
      <div class="row">
        <div class="col-xs-12">
          <div class="warning-box fs-12">
            Para empezar a vender en Mercado Libre primero necesitas confirmar la siguiente información:

            <ul>
              <% if current_retailer.id_number.blank? %><li>Identificación</li><% end %>
              <% if current_retailer.address.blank? || current_retailer.city.blank? || current_retailer.state.blank? %><li>Dirección</li><% end %>
            </ul>

            Puedes confirmar esta información directamente en Mercado Libre haciendo click <a href="https://www.mercadolibre.com" target="_blank">aquí</a>.</br></br>

            Nota: Si ya actualizaste tus datos por favor cierra sesión y vuelve a ingresar a Mercately.
          </div>
        </div>
      </div>
    <% end %>

    <div class="container-fluid">
      <div class="row my-perc-10">
        <div class="col-xs-12 col-md-12 mt-15">
          <div class="col-xs-12 pt-20 t-center">
            <%= image_tag 'mercado-libre-logo-5.png', class: 'logo-integration-platform' %>
          </div>
          <div class="col-xs-12 col-md-12 btn-box">
            <% if current_retailer.meli_retailer %>
              <p class="c-green fs-18 fs-sm-and-down-12">Conectado con Mercado Libre Exitosamente</p>
            <% else %>
              <%= link_to 'Conectar con Mercado Libre', ec_auth_url, class: 'fs-sm-and-down-8 fs-12 btn-btn btn-submit' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<%= render 'pages/shared/onboarding' if params[:onboarding] %>
