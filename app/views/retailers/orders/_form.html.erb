<div class="box">
  <div class="row">
    <%= form_for [:retailers, @order], html: { class: 'col-xs-12' } do |f| %>
      <div class="box">
        <div class="row">
          <div class="group col-xs-12 col-md-6">
            <%= f.select :status, Order.statuses.keys.to_a, include_blank: false %>
            <%= f.label :status, class: 'select-label' %>
            <span class="bar"></span>
          </div>
          <div class="group col-xs-12 col-md-6">
            <%= f.select :customer_id, @retailer.customers.pluck(:email, :id), include_blank: false %>
            <%= f.label :customer_id, class: 'select-label' %>
            <span class="bar"></span>
          </div>
          <h3>Items en la orden</h3>
          <div id="order_items" class="w-100">
            <%= f.fields_for :order_items do |oi| %>
              <%= render "order_items_fields", f: oi %>
            <% end %>
            <div class="links">
              <%= link_to_add_association 'Añadir item', f, :order_items, partial: 'order_items_fields' %>
            </div>
          </div>

          <div class="btn-box col-xs-12">
            <%= f.submit 'Guardar', class: 'btn-btn btn-submit' %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
